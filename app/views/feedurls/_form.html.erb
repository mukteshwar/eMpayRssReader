<%= form_for(@feedurl) do |f| %>
  <% if @feedurl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedurl.errors.count, "error") %> prohibited this feedurl from being saved:</h2>

      <ul>
      <% @feedurl.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :feed_title %><br />
    <%= f.text_field :feed_title %>
  </div>
  <div class="field">
    <%= f.label :feed_url %><br />
    <%= f.text_field :feed_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
